"""Defines an integer addition function with implicit typecasting and default parameter.

The add_integer() function returns the sum of two numbers, implicitly casting 
floats to ints. Integers can be expressed as literal ints, floats, or strings.
If only one parameter is given, it adds 98 by default.

Usage:

add_integer(2, 3)
    Returns 5
    
add_integer(2) 
    Returns 100

add_integer(2.3, -3)
    Returns -1
    
Parameters:

a: First number to add
b: Second number to add, defaults to 98

The function handles:

- Implicit typecasting from float to int.
- Default second parameter if not specified.
- Validates inputs are numbers and raises exceptions on invalid types.

Exceptions:

TypeError - If either argument is non-numeric
OverflowError - If infinity or NaN is passed
ValueError - If NaN is passed

"""
